# Add version command

Create a version command that prints the package version from package.json.
Add it as an npm script `version` in package.json.
Include a test that verifies it returns a valid semver string.
